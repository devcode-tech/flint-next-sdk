import React from "react";
import { FormFieldProps } from "@/lib/types";
export declare function TelephoneField({ field, value, onChange, onBlur, error, className }: FormFieldProps): React.JSX.Element;
